[build]
builder = "NIXPACKS"
buildCommand = "pip install -r api/requirements.txt && python -m spacy download en_core_web_lg"

[deploy]
startCommand = "uvicorn api.main:app --host 0.0.0.0 --port $PORT"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
