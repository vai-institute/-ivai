[build]
builder = "NIXPACKS"
buildCommand = "pip install -r api/requirements.txt && python -m spacy download en_core_web_lg"

[deploy]
startCommand = "sh -c 'cd /app/api && uvicorn main:app --host 0.0.0.0 --port $PORT'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3